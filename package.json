{
  "name": "sabha-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "validate": "node scripts/validate.mjs",
    "allowlist": "node scripts/allowlist.mjs",
    "clean": "pnpm -r exec rm -rf dist .tsbuildinfo",
    "server:start": "node packages/mcp-server/dist/index.js",
    "gen-key": "node scripts/gen-user-key.mjs",
    "gen-hash": "node scripts/gen-user-key.mjs hash"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
